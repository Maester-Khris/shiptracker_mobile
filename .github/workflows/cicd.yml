name: Build and save apk
on:
  push:
    branches:
      - master
jobs:
  build: 
    name: Greet from workflow
    runs-on: ubuntu-lastet
    steps:
        - uses: actions/checkout@v2
        - name: Run a multi-line script
          run: |
            echo Add other actions to build,
            echo test, and deploy your project.
      
